{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // don't know why need explicit typeroots but otherwise it fails
    "typeRoots": ["node_modules/@types"]
  }
}
{
  "extends": "./tsconfig.c.json",
  "include": ["src/**/*.ts", "src/**/*.tsx", "*.js"]
}
